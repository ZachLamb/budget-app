# Project skills (budget-app)

Cursor loads skills from `.cursor/skills/<name>/SKILL.md`.

| Skill | Use when |
|-------|----------|
| `budget-app-verify` | Full `./scripts/ci-local.sh` before merge-ready claims |
| `budget-app-quality` | After frontend edits (`npm run quality:check`) |

Personal cross-repo skills live in `~/.cursor/skills/` (copies of `~/.claude/skills/`).
