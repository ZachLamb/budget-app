# Test/dev-only dependencies — NOT installed in the production image.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=9.1.1
pytest-asyncio>=1.4.0
# Backs the in-memory SQLite engine the test suites use when Postgres
# isn't available (CI, isolated unit tests).
aiosqlite>=0.22.1
