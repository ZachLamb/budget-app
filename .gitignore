.env
.env.*
!.env.example
!.env.demo
.worktrees/
# Local Claude permissions; never commit tokens or bearer secrets (see .cursor/rules).
.claude/settings.local.json
# Claude Code agent worktrees (session-local scratch checkouts).
.claude/worktrees/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/
node_modules/
.venv/
backend/.venv/
.next/
frontend/node_modules/
frontend/.next/
*.db
*.sqlite
.DS_Store
# Cursor IDE plan scratchpad (keep local only)
.cursor/plans/
caddy_data/
caddy_config/

# Created by local vercel-build-check; never commit
/node_modules
/public
desktop/Snack's Budget/

# SwiftPM build output and local IDE state (Package.resolved IS committed —
# it pins dependency versions).
desktop/.build/
desktop/.swiftpm/
# Xcode derived data and user state
*.xcuserstate
xcuserdata/
.neon

# Personal local compose overrides (host port clashes, etc.)
docker-compose.override.yml
.vercel

# Back-test inputs: real filed-return figures, never committed
backend/tests/backtest/returns.local.json
